{
  "name": "tapedeck-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "eslint .",
    "pretty-all": "npx prettier --write ./function/**/*.js",
    "pkg": "rm -rf ./dist/${FN}/ && mkdir -p ./dist/${FN}/ && cp -f ./function/${FN}/package*.json ./dist/${FN}/ && cp -f ./function/${FN}/${FN}.js ./dist/${FN}/index.js && cd ./dist/${FN}/ && npm install --production && zip -r function.zip index.js node_modules",
    "upload": "aws lambda update-function-code --function-name tapedeck-${FN} --zip-file fileb://dist/${FN}/function.zip",
    "request:build": "FN=request npm run request:pkg && npm run request:upload",
    "process-ddb-stream:build": "FN=process-ddb-stream npm run process-ddb-stream:pkg && npm run process-ddb-stream:upload",
    "archive2:build": "FN=archive2 npm run archive2:pkg && npm run archive2:upload",
    "process-s3:build": "FN=process-s3 npm run process-s3:pkg && npm run process-s3:upload",
    "all:build": "npm run request:build && npm run process-ddb-stream:build && npm run archive2:build && npm run process-s3:build"
  },
  "devDependencies": {
    "@types/mocha": "^8.2.0",
    "@types/node": "^14.14.9",
    "aws-sdk": "^2.892.0",
    "eslint": "^7.16.0",
    "eslint-config-prettier": "^6.15.0",
    "eslint-plugin-prettier": "^3.1.4",
    "prettier": "^2.2.0",
    "typescript": "4.0.3"
  },
  "eslintConfig": {
    "root": true,
    "env": {
      "es2020": true,
      "node": true,
      "mocha": true
    },
    "plugins": [
      "prettier"
    ],
    "extends": [
      "eslint:recommended",
      "prettier"
    ],
    "rules": {
      "strict": "error",
      "semi": "error",
      "prettier/prettier": "error",
      "no-unused-vars": [
        "error",
        {
          "argsIgnorePattern": "^_"
        }
      ]
    },
    "ignorePatterns": [
      "node_modules/",
      "dist/"
    ]
  },
  "prettier": {
    "singleQuote": true,
    "trailingComma": "none",
    "quoteProps": "consistent"
  }
}
