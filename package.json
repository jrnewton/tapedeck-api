{
  "name": "tapedeck-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm run list:install && npm run archive:install",
    "lint": "eslint .",
    "pretty-all": "npx prettier --write ./function/**/*.js",
    "fninstall": "cd ./function/${FN}/ && npm install",
    "pkg": "rm -rf ./dist/${FN}/ && mkdir -p ./dist/${FN}/ && cp -f ./function/${FN}/package*.json ./dist/${FN}/ && cp -f ./function/${FN}/${FN}.js ./dist/${FN}/index.js && cd ./dist/${FN}/ && npm install --only=production && zip -r function.zip index.js node_modules",
    "upload": "aws lambda update-function-code --function-name tapedeck-${FN} --zip-file fileb://dist/${FN}/function.zip",
    "list:install": "FN=list npm run fninstall",
    "list:pkg": "FN=list npm run pkg",
    "list:upload": "FN=list npm run upload",
    "list:build": "npm run list:pkg && npm run list:upload",
    "archive:install": "FN=archive npm run fninstall",
    "archive:pkg": "FN=archive npm run pkg",
    "archive:upload": "FN=archive npm run upload",
    "archive:build": "npm run archive:pkg && npm run archive:upload"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/mocha": "^8.2.0",
    "@types/node": "^14.14.9",
    "eslint": "^7.16.0",
    "eslint-config-prettier": "^6.15.0",
    "eslint-plugin-prettier": "^3.1.4",
    "prettier": "^2.2.0",
    "typescript": "4.0.3"
  },
  "eslintConfig": {
    "root": true,
    "env": {
      "es2020": true,
      "node": true,
      "mocha": true
    },
    "plugins": [
      "prettier"
    ],
    "extends": [
      "eslint:recommended",
      "prettier"
    ],
    "rules": {
      "strict": "error",
      "semi": "error",
      "prettier/prettier": "error",
      "no-unused-vars": [
        "error",
        {
          "argsIgnorePattern": "^_"
        }
      ]
    },
    "ignorePatterns": [
      "node_modules/",
      "dist/"
    ]
  },
  "prettier": {
    "singleQuote": true,
    "trailingComma": "none",
    "quoteProps": "consistent"
  }
}
